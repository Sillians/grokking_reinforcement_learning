# Baseline config for __PAPER_ID__
paper_id: "__PAPER_ID__"
paper_title: "__PAPER_TITLE__"
seed: 42

environment:
  id: "TODOEnv-v0"
  max_steps: 1000

algorithm:
  name: "TODO_ALGO"
  learning_rate: 0.0003
  gamma: 0.99
  batch_size: 64

training:
  total_steps: 100000
  eval_interval: 5000
  num_eval_episodes: 10

logging:
  output_dir: "papers/__PAPER_ID__/experiments/results/baseline"
